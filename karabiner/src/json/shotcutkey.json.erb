{
  "title": "ShotCutKey98",
  "rules": [
    {
    "description": "RightOption + L_ダウンロードディレクトリへ98",
    "manipulators": [
      {
      "type": "basic",
      "from": <%= from("l", ["right_option"], ["caps_lock"]) %>,
      "to": <%= to([["open_bracket",["left_shift", "left_option"]]]) %>
      }
    ]
    }
  ]
}