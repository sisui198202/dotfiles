{
    "title": "FrontmostIfPreview",
    "rules": [
    {
      "description": "{キー1} + {キー2}_ここから登録(Preview)",
      "manipulators": [
        {
          "type": "basic",
          "from": <%= from("u", ["left_alt"], ["caps_lock"]) %>,
          "to": <%= to([["l","right_gui"], ["c", "right_gui"]]) %>,
          "conditions": [ <%= frontmost_application_if("preview") %> ]
        }
      ]
    }
    ]
}